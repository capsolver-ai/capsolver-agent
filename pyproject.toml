[build-system]
requires = ["hatchling >= 1.27.0"]
build-backend = "hatchling.build"

[project]
name = "capsolver-agent"
version = "0.1.0"
description = "Agent integrations for CapSolver — framework-agnostic tool definitions + LangChain tools."
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.10"
keywords = ["capsolver", "captcha", "agent", "langchain", "ai-agent", "tools"]
authors = [{ name = "capsolver-ai", email = "support@capsolver.com" }]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Internet :: WWW/HTTP",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]
dependencies = [
    "capsolver-core>=0.1.0",
]

[project.optional-dependencies]
langchain = [
    "langchain-core>=0.3.0",
]
browser = [
    "playwright>=1.40",
]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=1.0",
    "ruff>=0.4",
    "mypy>=1.10",
]

[project.urls]
Homepage = "https://capsolver.com"
Repository = "https://github.com/capsolver-ai/capsolver-agent"
Issues = "https://github.com/capsolver-ai/capsolver-agent/issues"
Documentation = "https://github.com/capsolver-ai/capsolver-agent/blob/main/docs/agent-integration.md"
Changelog = "https://github.com/capsolver-ai/capsolver-agent/blob/main/CHANGELOG.md"
Security = "https://github.com/capsolver-ai/capsolver-agent/blob/main/SECURITY.md"

[project.scripts]
capsolver-agent = "capsolver_agent.__main__:main"

[tool.hatch.build.targets.wheel]
packages = ["src/capsolver_agent"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
target-version = "py310"
line-length = 120

[tool.mypy]
python_version = "3.10"
strict = true
